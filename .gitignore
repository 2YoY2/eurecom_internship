# Local overrides for versions.env (image tags, namespace)
versions.local.env

# Site configuration never belongs in this repo
config/
site.yaml
*.kubeconfig
kubeconfig*
.env

# Captures that carry host details, MACs, IPs
*.pcap
*-inventory-*.tar.gz
